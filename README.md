Simple grid display widget in Pico-8 to demonstrate how render method is independent of internal state.

Cursor position does not reset when changing view mode, showing how the new render methods plug into an existing state ad do not require rewriting of internal state processing.
