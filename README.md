Simple grid display implementation in Pico-8 to demonstrate how render methodis independent of internal state.

Cursor position does not reset when changing view mode, showing how the new render methods plug into an existing state.
